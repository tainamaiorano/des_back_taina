// Cria uma variável numérica com um valor
let numero = 7;

// Usa um operador ternário para verificar se o número é par ou ímpar
let resultado = (numero % 2 === 0) ? "O número é par" : "O número é ímpar";

// Exibe o resultado no console
console.log(resultado);
